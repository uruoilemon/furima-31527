<% if Favorite.exists?(item_id: @item.id) %>
  <td><%= link_to 'いいね外す', item_favorite_path(@item), method: :delete %></td>
<% else %>
  <td><%= link_to 'いいねする', item_favorites_path(@item), method: :post %></td>
<% end %>